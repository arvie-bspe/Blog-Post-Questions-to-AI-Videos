{
  "name": "article-video-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "worker": "node --use-system-ca --env-file-if-exists=.env.worker worker/studio-worker.mjs"
  },
  "dependencies": {
    "playwright": "1.63.0"
  }
}
